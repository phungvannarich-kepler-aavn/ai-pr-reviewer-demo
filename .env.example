# =============================================================================
# AI PR Reviewer Demo - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your API keys

# -----------------------------------------------------------------------------
# OpenRouter API Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Get your API key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-your-api-key-here

# LLM Model to use for reviews (Optional, default: anthropic/claude-3-haiku)
# Popular options:
#   - anthropic/claude-3-haiku (fast, cheap)
#   - anthropic/claude-3-sonnet (balanced)
#   - openai/gpt-4-turbo (powerful)
#   - google/gemini-pro (alternative)
LLM_MODEL=anthropic/claude-3-haiku

# -----------------------------------------------------------------------------
# GitHub Configuration (OPTIONAL but recommended)
# -----------------------------------------------------------------------------
# Required for:
#   1. Private repositories (to fetch PR diffs)
#   2. Posting review comments directly to GitHub PRs
#   3. Avoiding GitHub API rate limits
# 
# Create a token at: https://github.com/settings/tokens
# Scopes needed:
#   - Classic Token: 'repo' scope (or 'public_repo' for public repos only)
#   - Fine-grained Token: 'Contents: Read' and 'Pull requests: Read & Write'
GITHUB_TOKEN=ghp_your_github_token_here

# -----------------------------------------------------------------------------
# API URL Configuration (OPTIONAL)
# -----------------------------------------------------------------------------
# Only change this if running in a custom setup
API_URL=http://localhost:8000
